<?xml version="1.0" encoding="utf-8"?>
<!-- 默认圆角按钮样式 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 禁用状态 -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/public_button_round_size" />
            <solid android:color="@color/public_style2_color_button_disable" />
        </shape>
    </item>

    <!-- 按压状态 -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/public_button_round_size" />
            <solid android:color="@color/public_style2_color_button_pressed" />
            <stroke android:width="@dimen/public_line_size" android:color="@color/public_colorLine"/>        </shape>
    </item>

    <!-- 焦点状态 -->
    <item android:state_focused="true">
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/public_button_round_size" />
            <solid android:color="@color/public_style2_color_button_background" />
        </shape>
    </item>

    <!-- 默认状态 -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/public_button_round_size" />
            <solid android:color="@color/public_style2_color_button_background" />
            <stroke android:width="@dimen/public_line_size" android:color="@color/public_colorLine"/>
        </shape>
    </item>

</selector>